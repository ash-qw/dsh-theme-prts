# 变更记录

## 0.1.100 - 2026-09-22

- 为当前会话流束增加独立辉光开关、预设与自定义三色调色板、速度调节及实时预览。
- 修复 DSH Web `0.1.5-rc.1` 新会话中模型选择器因宿主类名哈希变化被误判为操作按钮、继而压缩至 36px 的问题；模型控件改用 ARIA 与可见文字语义识别。
- 串行执行发布前测试，避免多个构建测试争用同一个临时 bundle 导致偶发失败。

## 0.1.99 - 2026-09-20

- 修复主题对宿主浮层层级的干预，确保设置界面继续覆盖 SSH 等插件面板。
- 保留斜杠快捷命令的原生键盘选择与高亮反馈，避免 Composer 通用按钮样式覆盖候选项状态。
- 为当前会话增加持续运行的粉白、绿色与蓝色三线流束；悬停时仍由拾音器接管，并提供独立的主题设置开关与减少动态后备状态。

## 0.1.98 - 2026-09-14

- 将 Harness 原生会话样式设为默认首选，通讯链条样式调整为可选增强模式。
- 收紧旧版 Composer 的结构识别范围，避免覆盖 `dsh-partner` 伙伴面板的身份表单。
- 将左下角主题关闭叉号替换为带明确文案的电源退出按钮。
- 修复启动动画首帧短暂露出输入框的问题，并通过不透明合成层、连续进度曲线和延迟粒子首绘减少动画卡顿。

## 0.1.97 - 2026-09-14

- 适配已部署的 DeepSeek Harness Web `0.1.5-rc.1`（内置前端包 `0.1.5-rc.2`）的 `main` / `rightbar` 主布局槽位，同时保留 `0.1.2` 的 `conversation` / `details` 兼容路径。
- 适配新版 `main.conversation` 会话根与 `data-composer-input` contenteditable Composer，并保持旧版 textarea 输入框外观。
- 将 `engines.dsh` 扩展为经过验证的 `0.1.2` 与 `0.1.5` 两条发布线。

## 0.1.96 - 2026-09-07

- 修复输入框粘贴图片后预览被 Composer 玻璃层遮挡的问题，并为 `display: contents` 附件槽增加像素级回归测试。
- 保留附件缩略图、删除键与轮播按钮的宿主原生圆角、轮廓和尺寸，避免通用 Composer 控件样式误伤附件区域。
- 通过 `engines.dsh` 声明 DeepSeek Harness Web `>=0.1.2-rc.1 <0.1.3-0` 兼容范围，并在中英文文档中说明已验证版本与插件市场识别方式。

## 0.1.95 - 2026-09-04

- 适配 DeepSeek Harness Web `0.1.2-rc.1`：迁移到新的客户端 Store 模块，避免插件在启动阶段因旧 runtime 缺失而加载失败。
- 适配 `0.1.2-rc.1` 的命令候选弹层结构，让外层面板与候选列表继续使用一致的 P.R.T.S. 材质。
- 退役主题自建的会话刻度 DOM、滚动接管、预览与校准设置，完整保留 Harness 原生 TurnNavigator 的结构、行为与外观，并移除会话正文前的纵向导轨和菱形标记。
- 将 Playwright 开发依赖升级至 `1.55.1`，消除发布前安全审计告警。

## 0.1.94 - 2026-09-03

- 修复工作区与会话悬停动画触发横向滚动条，避免侧栏尺寸跳变。
- 插件基础设置改为基于实际容器宽度响应，在手机和窄面板中自动纵向排列。

## 0.1.93 - 2026-09-02

- 将安装说明改为直接从 npm 安装和更新。
- 新增用于 npm 检索的关键词元数据。
- 新增普通会话粒子徽记 0×–2× 跟随移动速度设置，支持视口周期提示、实时预览、净滚动位移触发及旧偏好迁移。
- 固定长对话会话刻度栏的几何基准，避免位置随内容高度和刷新偏移。
- 保留 Harness 宿主弹窗按钮的原生前景与背景配色，避免主题切换后文字不可见。
- 将维护者发布流程从用户 README 移至独立文档。

## 0.1.92 - 2026-08-31

- 通过 npm Trusted Publishing 发布首个正式公开版本，并生成来源证明。
- 对齐 npm 包名、Host patch 包名和客户端 ModuleLoader ID。

## 0.1.91 - 2026-08-31

- 发布用于在 npmjs 建立包及 Trusted Publisher 关联的引导版本。

## 历史开发版本

以下版本早于当前受支持的 npmjs 公开发布线：

- `0.1.35` 从过时代码分支构建，`cordis.patch.yml` 使用了错误的包名。
- `0.1.66` 修复了 Host patch 包名，但没有同步客户端 ModuleLoader ID。
- `0.1.67` 对齐了三处包名。
- `0.1.68` 在 Operations Shell 延迟挂载期间继续保持基础主题生效。
- `0.1.69` 不再占用宿主共享的详情侧栏。
- `0.1.70` 新增工作区城市剪影、会话录音声纹、刷新启动接管和结构完整的正交粒子徽记。
- `0.1.73` 保留常规浮层接管与 composer 越界显示，让启动动画主题样式立即生效，并将非启动工作延后至宿主原生启动完成。
